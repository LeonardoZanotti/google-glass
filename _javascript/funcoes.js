function mudaFoto(foto) {										/* Função com nome "mudaFoto" que ao chamada executa os seguintes códigos: */
		document.getElementById("icone").src = foto;			/* Busca um documento com id="icone" e muda ele para foto (foto é o mesmo que está ao lado da função, então quando chamado pode-se alterar o documento através do parâmetro foto */
	}